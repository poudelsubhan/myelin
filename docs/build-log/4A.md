# 4A — Repair, reconciliation and core demonstration

2026-09-08. A retained live session resumes from the failed write checkpoint.
Pre-write 422 rejection is definitive; ambiguous transport failures are reconciled
through the normal authenticated operation endpoint. Applied writes restore their
response-derived Location and resume the suffix. Absent/pending results remain
unknown without handler-completion evidence. No general undo is attempted.

Presentation-only compatibility preserves HTTP operation while a real field
contract change triggers the invoice write failure. Visual repair uses the same
operation identity for the rejected request. The model compiler must preserve all
unscoped steps exactly; the changed candidate passes all eight changed-revision
cases before restoration promotion.

Initial repair gate passed: 0813c45d-b974-4529-96c6-93322202a428.
Repaired candidate: 940f61ecd6088081b98cb361389598f87c4d57a969dd2959b4e84fcd0303ad97.
Zero-call replay and injected lost-response reconciliation both passed the oracle.
Core CLI assertions and two clean rehearsals passed before the named
core checkpoint was frozen for extension phases.

First complete clean rehearsal: demo-3bb22721-777a-4484-a0ed-4fba67fe6a55,
C1–C8 passed, then independently audited against trace/action/frame/usage files,
immutable hashes, exact repair scope, the 422 failure, eight-case gates and
rejection decisions. A labelled real-event fallback recording is retained with
its source-run manifest in that demo directory.

Second attempted rehearsal found a test-construction defect: the model marked a
UI fill as a write with an operation key, and the redundant-action candidate copied
that identity. The schema correctly rejected the duplicate. The fixture generator
now selects a fill explicitly and allocates a fresh identity. A regression test
covers this variant. The failed rehearsal is retained rather than counted as pass.

Second complete clean rehearsal: demo-4f8b5c0d-419d-4648-94e1-4a4a2dcbeafc.
All C1–C8 assertions and the independent artifact audit passed. Final offline
checks: 53 tests passed; Ruff passed. The `core-demo` tag preserves this milestone.
