# 2A — Observed HTTP dependency lifting

2026-09-08. Conservative form lifter correlates a mutating POST's CSRF value with
an earlier HTML response and requires the observed action_url to match the POST.
Hidden action URLs expose already-present form actions without hardcoded IDs.
Declared field names disambiguate repeated equal inputs; derived totals use typed
integer multiplication. Login/customer selection remain UI steps.

Actual source trace: 65150b6a-d1cc-4d0b-a53f-8497d69ba410. The lifter produced two
prerequisite GETs and two HTTP writes, with real request/action IDs. Response
Location binds the next entity page; fresh hidden CSRF/action URL extraction binds
writes. Missing sources or unexplained fields do not become high-confidence code.

The handwritten evidence program passed all eight cases on separate tenants with
fresh sessions and generated IDs. Maximum work: 68 units. No model calls in these
program executions. This is Phase 2 infrastructure evidence, not model compilation.
