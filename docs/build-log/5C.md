# 5C — Differential validation and native async tools

A full gate runs eight fresh program contexts and eight fresh model contexts with
the same seed, environment and policy. Each passes the independent oracle before
relationship-preserving business projections are compared. Missing, failed,
zero-model or mismatched reference results cannot pass. Model usage is retained
under reference purpose with unique response IDs.

Native async calls own an immutable GateRequest, original call ID and unique
model-supplied task handle. Completed identities remain reserved. Tests cover
out-of-order completion, duplicates, failure, cancellation and the status-only wait
output. Compiler tool providers dispatch async starts before synchronous waits.
Effort updates preserve request-level effort and record effective effort locally.

Live verification is in progress. The initial direct-call check incorrectly
assumed no following wait call; the API had returned both. A corrected direct-call
probe demonstrated real pending work and an accepted effort update, but several
model-reference calls hit API InternalServerError. That gate remains failed.
Transient transport/429/5xx retries are now bounded and recorded; uncertain usage
is reported separately from known response costs. No failed reference is omitted.

First full result: gate 0ed6ff1e-5140-48e6-a194-dac26fe131a4, 8/8 program
oracle checks and 5/8 reference comparisons. Three InternalServerError outcomes
kept the gate failed. Native call call_8jflg1uwibE2MGvZWFhB5L84 received one
terminal output. Independent analysis resp_071c37781c7cb30d006aa06f333f8487d0bbccbf917f7be4e5
completed while the gate was pending, with effective high effort and unchanged
request-level medium effort. These protocol successes do not turn the failed
full correctness gate into a pass.

Core regression demo-d76a1155-7330-48d5-b248-e35b797e117b passed all C1–C8.
A final integrated-runtime rehearsal and a fresh full reference gate are running.

Fresh full gate passed: 1ee95c6f-8643-45e4-98d7-6f3a7c626008. All eight
program oracles and all eight fresh model references passed and matched. Probe
phase5-native-5df7bb29-c615-4314-a5ee-07ac2061a1fd retained the immutable hash,
original async call ID, exactly one terminal output, useful analysis while pending,
and accepted high-effort continuation with unchanged medium request effort.

Final orchestrator-owned live proof: run 50cf2b09-9330-477b-beca-474b4148b629,
gate 10f6f947-fa71-42ae-a623-c86c6097f08a, 8/8 program and 8/8 reference
passes/matches. `scripts/check_phase5.py` now POSTs to `/native-gates`; the server
owns model calls, browser contexts, analysis events and gate jobs. Original async
call: call_ms5LFz1bYazDPln30OypywMg. Exactly one terminal output, independent
analysis while pending, and accepted effort update were verified again.

Final core regression demo-48be4089-e731-4877-b7cb-fe45625b27a5 passed all C1–C8.
Offline checks: 74 tests and Ruff passed. The user confirmed a one-minute demo
video requirement. A reported port-8101 concern was checked: health/login responded
in 1–16 ms; root returned 404. Root now redirects to login and was verified live.
Quiet access logging avoids accumulating unnecessary request logs during gates.
