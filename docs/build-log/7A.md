# 7A — Full demonstration and video

Full CLI mode composes the retained core workflow and all extension checks. Fresh
mode records new source traces and obtains current candidate/environment values;
it does not depend on the historical private run IDs used during development.
A separate `--reuse-evidence` path audits actual prior live proofs and explicitly
labels their reuse. It sends only program-mode requests with compilation and full
reference validation disabled.

The participant asked to conserve a $120 daily API allowance and avoid excessive
browser testing. Accordingly, final rehearsals each used three fresh headless tasks
with zero model calls. No repeated model-reference suite was launched in Phase 7.
The two successful rehearsals are `full-demo-20b4aeea-d930-4b08-b0bf-bfec164d1dfa`
and `full-demo-b5b366f4-3c71-490b-a349-8fd3d344d43f`. Each audits C1–C8/E1–E5,
checks the final artifacts, and exercises repaired CRM/consent/expense programs.
The [export](../evidence/phase7.json) reports original and new evidence separately.

The one-minute video uses actual saved screenshots, program fields, oracle results
and source IDs. It is labelled recorded evidence and edited excerpts. Local macOS
narration and a local encoder used no model/media APIs. Encoded duration: 60.00 s;
full video/audio decoding passed. Frames were visually inspected and clipped code
text corrected. [Manifest](../media/manifest.json) records the video SHA-256 and
per-scene provenance. The fixture rendering browser did not interact with live apps.

Offline checks passed: 88 tests and Ruff. The core JSON-only demo remains preserved
at `core-demo` and passed its final Phase 6 regression on the integrated runtime.
