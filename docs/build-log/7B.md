# 7B — Public packaging and development evidence

README and runbook explain both owned workflows, source-linked compilation,
independent finite-suite verification, effect reconciliation and promotion policy.
They distinguish supported local apps from general SaaS compatibility and clearly
label fresh model runs versus budget-conserving evidence reuse.

Selected sanitized coding-session evidence includes the dropped repair-policy bug,
its proposed fix and regression test, plus the explicit-navigation runtime fix.
The private phase plan and raw session materials remain ignored. The public file
scan checks the configured API key and any non-template private admin token before
publication; it passed. No secrets or private raw artifacts are exported.

The participant supplied the one-minute requirement and submission link. The final
video meets that duration and is ready for their submission workflow. Other
participant-guide eligibility details and finalist timing are not asserted as
verified. Nothing was enrolled, signed, paid or submitted automatically.

Clean-clone verification is performed after committing these packaged artifacts;
its result is recorded below. It uses offline lint/tests and checks the existing
video/manifest, without API calls or browser test suites.
